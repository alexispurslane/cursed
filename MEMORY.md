# Project Memory

This file is auto-maintained by the pi-memory extension.
It stores user preferences, key decisions, durable gotchas, and frequently-referenced files
so the agent can pick up context across sessions.

## Heavily Read

- src/cursed/buffer.lua (3 reads) — Let me examine the `delete_char` implementation in buffer.lua - this is likely w
- src/cursed/view.lua (6 reads) — Now let me read the View's `delete_char` and `delete_selection` methods.
