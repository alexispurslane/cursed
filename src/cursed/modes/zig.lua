--- Zig major mode (built-in).
--- (No bundled tree-sitter-zinc grammar yet; highlighting disabled.)

---@return MajorModeSpec
return {
    name = "zig",
    tab_width = 4,
    expand_tab = true,
    indent_width = 4,
}
